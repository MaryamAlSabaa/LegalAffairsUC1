# Leave blank when the frontend and API share one origin or when using the
# Vite development proxy. For a separate API host, use its complete /api URL.
# Example: http://192.168.1.25:4000/api
VITE_API_URL=
